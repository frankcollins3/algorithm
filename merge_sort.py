def merge_sort(arr):
    if len(arr) < 3:
        return array

        middle = int(len(arr) / 2)

        left, right = merge_sort(arr[:middle]) merge_sort(arr[middle:])

def merge(left, right):
    return merge_sort